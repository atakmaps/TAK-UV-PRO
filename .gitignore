# Gradle
.gradle/
build/
local.properties
# gradle.properties is tracked (shared JVM/AndroidX flags); keep machine-only overrides out of commits if you prefer.

# TAK dev plugin tooling (timestamp/hash churn — regenerated per dev machine)
.takdev/plugin.properties

# Android Studio / IDE
.idea/
*.iml
.vscode/

# ATAK SDK (downloaded separately — see README)
app/libs/atak-civ/
ATAK-CIV-*/
*.zip

# Signing
*.keystore

# Build outputs
app/build/
*.class
*.dex
*.apk
*.ap_

# Local-only agent / handoff notes (not published)
NEW-AGENT-HANDOFF.md

# Debug / scratch
build_output.txt
jar_list.txt
# Local logs/diagnostics — keep README; ignore everything else in scratch/
*.log
logs/
scratch/*
!scratch/README.txt

# Generated portal drop (tools/package-submission.sh)
submission/

# OS files
Thumbs.db
.DS_Store
*.swp
*.swo
*~
